<%= form_for(vizu) do |f| %>
  <% if vizu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vizu.errors.count, "error") %> prohibited this vizu from being saved:</h2>

      <ul>
      <% vizu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
