<%= form_for(note) do |f| %>
  <% if note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :investor_id %>
    <%= f.text_field :investor_id %>
  </div>
s
  <div class="field">
    <%= f.label :invest_date %>
    <%= f.date_select :invest_date %>
  </div>

  <div class="field">
    <%= f.label :note_type %>
    <%= f.collection_select :note_type, Notetype.all, :note_type, :note_program, prompt: true %>
  </div>

  <div class="field">
    <%= f.label :dollar_invest %>
    <%= f.number_field :dollar_invest %>
  </div>

  <div class="actions">
    <%= f.submit %>
    <%= `python #{Rails.root + "/scripts/dist_schedule.py"}` %>
  </div>
<% end %>
