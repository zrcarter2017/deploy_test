<%= form_for(note) do |f| %>
  <% if note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :investor_id %>
    <%= f.text_field :investor_id %>
  </div>

  <div class="field">
    <%= f.label :invest_date %>
    <%= f.text_field :invest_date %>
  </div>

  <div class="field">
    <%= f.label :note_type %>
    <%= f.text_field :note_type %>
  </div>

  <div class="field">
    <%= f.label :dollar_invest %>
    <%= f.text_field :dollar_invest %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
