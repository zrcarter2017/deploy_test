<%= form_for(distribution) do |f| %>
  <% if distribution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(distribution.errors.count, "error") %> prohibited this distribution from being saved:</h2>

      <ul>
      <% distribution.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :note_id_id %>
    <%= f.text_field :note_id_id %>
  </div>

  <div class="field">
    <%= f.label :quart_num %>
    <%= f.text_field :quart_num %>
  </div>

  <div class="field">
    <%= f.label :dist_date %>
    <%= f.date_select :dist_date %>
  </div>

  <div class="field">
    <%= f.label :cum_sum_days %>
    <%= f.text_field :cum_sum_days %>
  </div>

  <div class="field">
    <%= f.label :quart_days %>
    <%= f.text_field :quart_days %>
  </div>

  <div class="field">
    <%= f.label :dollar %>
    <%= f.text_field :dollar %>
  </div>

  <div class="field">
    <%= f.label :interest_rate %>
    <%= f.text_field :interest_rate %>
  </div>

  <div class="field">
    <%= f.label :distribution %>
    <%= f.text_field :distribution %>
  </div>

  <div class="field">
    <%= f.label :paid %>
    <%= f.text_field :paid %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
